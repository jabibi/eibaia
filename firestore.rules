rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the FastAPI backend (Admin SDK), which
    // bypasses these rules entirely. Direct client access is never used.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
